fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
supabase==2.5.1
httpx==0.27.0
pyjwt==2.8.0
python-multipart==0.0.9
python-dotenv==1.0.1

# Used in the feature-build phase (kept here so the Docker image already
# has them, avoiding a slow rebuild later):
beautifulsoup4==4.12.3
playwright==1.45.0
weasyprint==62.3
